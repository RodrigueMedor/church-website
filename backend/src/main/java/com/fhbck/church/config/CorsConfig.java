package com.fhbck.church.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class CorsConfig {
    // CORS is configured in SecurityConfig.java via cors.customizer
}
